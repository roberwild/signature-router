# =============================================================================
# Signature Router Admin - Variables de Entorno
# =============================================================================
# Copia este archivo como .env.local y configura los valores apropiados
# cp env.local.example .env.local (Linux/Mac)
# copy env.local.example .env.local (Windows)

# NextAuth Secret (genera uno nuevo para producción con: openssl rand -base64 32)
AUTH_SECRET="<genera-un-secret-aleatorio>"

# URL de la aplicación
NEXTAUTH_URL="http://localhost:3000"

# =============================================================================
# Keycloak - Solicita las credenciales al equipo de infraestructura
# =============================================================================
# Opción A: Keycloak LOCAL (Docker)
# KEYCLOAK_CLIENT_ID="signature-router-admin"
# KEYCLOAK_CLIENT_SECRET="<secret-del-keycloak-local>"
# KEYCLOAK_ISSUER="http://localhost:8180/realms/signature-router"

# Opción B: Keycloak DESARROLLO (Active Directory)
KEYCLOAK_CLIENT_ID="<solicitar-a-infraestructura>"
KEYCLOAK_CLIENT_SECRET="<solicitar-a-infraestructura>"
KEYCLOAK_ISSUER="<solicitar-a-infraestructura>"

# =============================================================================
# Backend API
# =============================================================================
NEXT_PUBLIC_API_BASE_URL="http://localhost:8080/api/v1"
NEXT_PUBLIC_USE_MOCK_DATA="false"
NEXT_PUBLIC_DEBUG="true"
